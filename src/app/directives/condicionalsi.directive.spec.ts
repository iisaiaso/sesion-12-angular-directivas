import { CondicionalsiDirective } from './condicionalsi.directive';

describe('CondicionalsiDirective', () => {
  it('should create an instance', () => {
    const directive = new CondicionalsiDirective();
    expect(directive).toBeTruthy();
  });
});
  